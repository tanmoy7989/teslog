# Secrets and local config
.env
.env.local
secrets/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data volumes (bind mounts) — anchored to the repo root: an unanchored "data/" also matches
# tests/data/, which holds committed SQL fixtures, not a runtime volume.
/data/

# Downloaded tools (see scripts/get-tesla-tokens.sh)
bin/
.venv-tesla-auth/
.venv-test/

# Test artifacts
tests/screenshots/

# Drive export cron log (see scripts/export-to-drive.sh)
drive-export.log

# OS / IDE
.DS_Store
.idea/
.vscode/

# Docker
docker-compose.override.yml
